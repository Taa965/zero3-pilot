# Zero3 Pilot v0.1.0-alpha — Draft Release Notes

> **Draft only — not a published release.**
>
> These notes describe the current first-alpha release candidate. PRs #49 and #51 are merged, but the final release SHA, exact-candidate Windows artifact/checksum, Git tag and GitHub pre-release are not yet published. Replace this notice only after the release process is complete.

## What this alpha is

`v0.1.0-alpha` is intended to establish a reviewable Codex-native desktop-agent foundation, not to claim production readiness.

The defining architectural choice is simple: **OpenAI's open-source Codex app-server remains the authoritative native Agent Kernel/runtime.** Zero3 Pilot adds a desktop product layer, integration guards, persistence/reliability work and controlled executor/handoff infrastructure around that core instead of replacing it with a second hidden agent loop.

The first-alpha scope is intentionally smaller than the broader roadmap. The formal ACP external executor (#48) is deferred because its own cross-platform behavior gate is red. A smaller truthful release is preferred to merging a provider integration with unresolved permission/failure semantics.

## Highlights on `main`

### Codex-native desktop conversation path

The Hermes-derived Electron/React UI has a Zero3-owned typed bridge to the pinned open-source Codex app-server. The migrated path includes:

- Thread start/list/read/resume and durable conversation semantics;
- Turn start/interrupt and streaming Item projection;
- native command/file approvals and user-input prompts;
- reasoning, command, file-change, MCP, dynamic-tool, plan and web-search presentation;
- structured user input and supported local images;
- thread archive/unarchive/delete/rename/fork and active-turn steer;
- authoritative message/Turn mapping and paginated history for destructive history operations.

The Renderer is not given a generic Codex method tunnel; new capabilities require reviewed typed surfaces.

### Durable Zero3 AppServer session identity (#49)

[PR #49](https://github.com/Taa965/zero3-pilot/pull/49) is merged.

Pinned Codex separates the AppServer transport from the persisted session source. Zero3 now explicitly launches its Codex child with `--session-source app-server`, and the Zero3 list boundary requests `sourceKinds: ['appServer']`. This keeps the Zero3 desktop conversation namespace separate from unrelated VS Code Codex history.

The release-blocking regression smoke mirrors production launch arguments: create two non-ephemeral Threads, start a first Turn on each so normal durable history is materialized, restart app-server with the same `CODEX_HOME`, then require both original IDs through AppServer-source `thread/list` and `thread/read`.

### Reproducible pinned-Codex integration

Zero3 Pilot pins the open-source Codex source and maintains deterministic overlay/replay infrastructure so project-specific extensions and narrowly justified patches can be reviewed independently of the upstream gitlink.

CI includes a real Codex Core Smoke that builds the pinned Codex CLI and exercises a real `codex app-server` JSONL handshake rather than treating the integration as a mock-only boundary.

### Context and output retention

The alpha baseline includes two Codex-native resilience features:

- **D1:** lossless spill/recovery for oversized plain-text tool results with bounded model-context projection;
- **D2:** deterministic pruning of oversized historical tool results only in private compaction input, with D1-backed recoverability and no mutation of authoritative Thread history.

### Remote Host reliability foundation

The H0-H5 work establishes a narrow Remote Host path while preserving Codex execution authority:

- durable local task-to-Codex identity;
- crash-safe pending Turn recovery;
- durable bounded outbox with ack-before-delete;
- strict no-overtake publication ordering;
- authenticated durable control-plane state;
- sticky leases, fencing generations and replay/terminal idempotency.

### Executor, handoff and failover foundation

The current `main` also contains:

- `zero3.pilot.executor.v1`, a provider-neutral Executor contract;
- a durable Handoff protocol with deterministic workspace/Git checkpoint evidence and an exclusive writer gate;
- failover policy with bounded retry, cooldown, circuit-breaker and recovery-first semantics;
- a Native Codex executor backed by the pinned Codex app-server, using supported account/rate-limit APIs and explicit permission forwarding without reading/copying Codex credential files.

### Bundled pinned-Codex Windows package (#51)

[PR #51](https://github.com/Taa965/zero3-pilot/pull/51) is merged.

The Windows packaging path builds the exact reviewed Codex pin in release mode, places `codex.exe` under application resources, uses the bundled binary in packaged mode, carries required Zero3/OpenAI Codex/Hermes license and NOTICE material, builds an NSIS installer with publishing disabled in CI, runs `--version` plus a real app-server JSONL smoke against the packaged Codex binary, and computes an installer SHA-256.

Packaged mode is intentionally not allowed to silently switch to PATH, `@latest`, runtime download or an arbitrary `ZERO3_CODEX_BIN` host override.

## Integrated pre-tag Windows evidence

The #51 pull-request merge candidate already contained the merged #49 tree and therefore exercised both first-alpha implementation blockers together.

That Windows Alpha Artifact run passed package creation, bundled-runtime checks, legal-resource checks and the real packaged app-server smoke. Its validation artifact was:

```text
Zero3Pilot-0.1.0-alpha-win-x64.exe
SHA-256: 8DDC4EE5ED6A96E474625A1509779783E591E4F7B8F4473E2DF54E81868DB096
```

This checksum is recorded only as **pre-tag integrated validation evidence**. It is not the public `v0.1.0-alpha` checksum because the documentation closeout changes the final repository SHA and the exact release candidate must be revalidated before publication.

## Remaining release closeout

The implementation blockers are resolved. The remaining gate is release evidence on one exact final SHA:

1. merge the documentation closeout;
2. freeze the resulting exact `main` candidate;
3. confirm the exact Codex/Hermes/DeepSeek-Harness pins and license/NOTICE/provenance obligations;
4. run/confirm all required architecture, Codex, Windows target-shell and feature-specific release gates against that candidate;
5. produce the final Windows NSIS artifact from that exact candidate, verify the bundled pinned Codex runtime and record the final installer SHA-256;
6. keep unsigned/SmartScreen status explicit if no signing credential is used;
7. create the exact `v0.1.0-alpha` tag and GitHub pre-release, attach only evidence-matched artifacts, then verify tag/SHA/artifact/checksum links after publication.

Until those facts exist, this file remains Draft and the repository remains pre-release.

## Explicitly deferred: PR #48 ACP external executor

[PR #48](https://github.com/Taa965/zero3-pilot/pull/48) is **not included in `v0.1.0-alpha`**.

The dedicated R4B workflow currently passes architecture guards and strict TypeScript checks but fails its behavior suite on Ubuntu and Windows. The observed contract failures include:

- an explicit permission deny path ending as `succeeded` instead of `cancelled`;
- an ACP protocol-version mismatch being normalized as `unavailable` instead of `unsupported`.

The branch also predates the merged R4C Native Codex executor and needs replay/rebase onto the current authoritative stack. R4B will be repaired and revalidated in a later pre-release instead of weakening this alpha's security/failure semantics.

## Security model

This project touches high-privilege local surfaces, so security boundaries are part of the product architecture:

- unsupported server-request/permission classes fail closed;
- side-effecting behavior must pass reviewed permission boundaries;
- migrated core operations cannot silently route to a legacy runtime;
- external executors do not inherit task/router/handoff authority;
- Native Codex auth reuse uses supported Codex account APIs rather than token extraction or credential-file copying;
- a provider path with incorrect deny/failure semantics is deferred rather than waived into a release;
- packaged Windows builds must use the reviewed bundled Codex pin instead of arbitrary runtime substitution;
- security reports should use GitHub private vulnerability reporting as described in `SECURITY.md`.

## Known limitations

This is an alpha candidate. In particular:

- the project is not production-ready;
- no broad external adoption is claimed;
- some Hermes-derived backend behavior remains temporary compatibility scaffolding for UI surfaces not yet fully migrated; it is not a second native Agent Kernel, and migrated primary-chat execution remains Codex-owned;
- development conversations created before explicit Zero3 AppServer source tagging may have been stored under Codex's `vscode` source. `v0.1.0-alpha` does not promise automatic migration of those pre-release sessions because importing all `vscode` source rows could mix unrelated VS Code Codex history into Zero3;
- ACP external-agent execution is intentionally **not** part of this alpha;
- Remote Host -> Executor Manager/Handoff/Failover integration is still follow-up work;
- the Windows alpha is expected to be unsigned unless the final release evidence records an explicit signing step. Windows may therefore show SmartScreen or an unknown-publisher warning;
- polished screenshots/demo and the public installer checksum must come from the exact final release candidate rather than a mock or an older PR artifact.

## Windows distribution model

The first-alpha Windows target is an Electron/React NSIS package whose application resources include the reviewed Codex runtime at:

```text
resources/zero3-codex/codex.exe
```

The package also carries reviewed license/NOTICE material under its application resources. Some unported UI surfaces may still initialize the Hermes-derived local compatibility backend on first launch; this remains migration scaffolding, not the authoritative agent runtime.

The final installer filename, SHA-256 and release URL are intentionally omitted from the public-release claim until the exact final candidate artifact exists.

## Release checklist

Before turning this document into the GitHub Release body:

- [x] Merge #49 with explicit `--session-source app-server` + first-Turn cold-restart persistence semantics.
- [x] Merge #51 with bundled pinned Codex Windows packaging, legal notices, NSIS generation and packaged-binary app-server smoke.
- [x] Validate #49 + #51 together in the #51 pull-request merge candidate Windows Alpha Artifact gate.
- [x] Decide #48 scope: **deferred to a later pre-release** because its cross-platform ACP behavior tests are red.
- [x] Close or mark superseded audit/POC PRs so the authoritative implementation path is clear.
- [x] Mark `docs/DESKTOP_V3.md` as historical/superseded rather than current authority in this documentation closeout.
- [ ] Merge this final documentation closeout and freeze the resulting exact `main` SHA.
- [ ] Confirm the exact Codex/Hermes/DeepSeek-Harness pins at that release candidate.
- [ ] Require all release-blocking architecture, Codex, Windows target-shell and feature gates on the exact candidate.
- [ ] Re-run/confirm the real pinned-Codex app-server and session-persistence smokes on that exact candidate.
- [ ] Produce the actual Windows pre-release artifact from the exact candidate and record its final SHA-256.
- [ ] Verify third-party NOTICE/license/provenance obligations for the tagged tree and packaged artifact.
- [ ] Confirm whether the Windows artifact is signed; if unsigned, keep the SmartScreen/unknown-publisher limitation explicit.
- [ ] Confirm README, CHANGELOG, ROADMAP, SECURITY, CONTRIBUTING, Windows installation/deployment docs and this release body match the exact tag behavior.
- [ ] Capture real target-desktop screenshots/demo if a release-quality build is available and screenshots are included.
- [ ] Replace this Draft notice with the exact tag/date, publish the GitHub pre-release, and verify tag/SHA/artifact/checksum links.

## Project independence

Zero3 Pilot is an independent, unofficial open-source project and is not an OpenAI product. Codex is a project/trademark of OpenAI. Hermes Agent and DeepSeek-Harness remain governed by their respective upstream licenses and roles documented in this repository.

For the detailed architecture and roadmap, see [`../../README.md`](../../README.md), [`../../ROADMAP.md`](../../ROADMAP.md), [`../ARCHITECTURE_CONSTITUTION.md`](../ARCHITECTURE_CONSTITUTION.md) and [`../ARCHITECTURE.md`](../ARCHITECTURE.md).
