# skills/

Packaged, reusable task instructions Zero3 Pilot can load at runtime
(same concept as Claude Code Skills).

Current product skills:

- `zero3-web-worker/`: bounded Web GPT Worker behavior for the private Zero3 Worker Protocol.
